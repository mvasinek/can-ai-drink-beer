# can-ai-drink-beer

Monorepository used as source material for YouTube videos. Each top-level folder is a separate project or topic.

## Projects

### [tasks-mcp-server](tasks-mcp-server/)

Python backend for task management (SQLite, SQLModel, FastAPI). Currently at **v0.1.0** — core storage layer only.

### [semantic-versioning](semantic-versioning/)

Prompt templates for semantic version releases (major, minor, patch).
